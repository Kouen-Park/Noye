services:
  qdrant:
    # Pinned: the vector index and its API behavior must stay reproducible
    # across machines. Bump deliberately, then re-verify the collection setup.
    image: qdrant/qdrant:v1.19.1
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
