# Development and test dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
# Lint. Pinned like pytest: a linter that changes its mind between machines is a
# linter people start ignoring.
ruff==0.14.5
